# OS
.DS_Store

# Python
venv/
__pycache__/
*.pyc

# Local runtime config (may contain host/API details) — config.example.yaml is tracked instead
config.yaml

# exiftool backup files created on write
*_original
