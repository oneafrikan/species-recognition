# OS
.DS_Store

# Python
venv/
__pycache__/
*.pyc
.pytest_cache/

# Local runtime config (may contain host/API details) — config.example.yaml is tracked instead
config.yaml

# exiftool backup files created on write
*_original

# Local test images — never commit real photos
/test-photos/

# Claude Code session-runtime state, not project content
.claude/scheduled_tasks.lock
